@page
@model WeCareWebApp.Pages.PartnerPages.EditModel

<h4>BusinessClient</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Partner.Id" />
            <div class="form-group">
                <label asp-for="Partner.Name" class="control-label"></label>
                <input asp-for="Partner.Name" class="form-control" />
                <span asp-validation-for="Partner.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Partner.CreatedOnDate" class="control-label"></label>
                <input asp-for="Partner.CreatedOnDate" class="form-control" readonly />
                <span asp-validation-for="Partner.CreatedOnDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
