@page
@model WeCareWebApp.Pages.PartnerPages.IndexModel
@{
}
<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Partner[0].Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partner[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partner[0].Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partner[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partner[0].Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partner[0].MomoCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Partner[0].CreatedOnDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Partner)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MomoCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedOnDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
        }
    </tbody>
</table>